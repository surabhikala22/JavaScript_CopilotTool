{
  "name": "selenium-pom-framework",
  "version": "1.0.0",
  "description": "Selenium POM framework for login validation",
  "main": "index.js",
  "scripts": {
   // "test": "mocha tests/**/*.js --timeout 30000",
   "test": "mocha --reporter mocha-junit-reporter"
}
  },
  "config": {
    "timeout": 30000
  },
  "dependencies": {
    "jquery": "^3.7.1",
    "jsdom": "^26.1.0",
    "nock": "^14.0.5",
    "selenium-webdriver": "^4.34.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chromedriver": "^134.0.5",
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
